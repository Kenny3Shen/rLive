# Keep local Windows/WSL development on the installed stable channel. Release
# workflows pin their exact compiler version independently for reproducibility.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
