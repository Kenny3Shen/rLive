# mpv + Bilibili real-machine smoke (2026-07-24)

## Environment

- OS: Linux (Ubuntu noble packages)
- mpv: **0.37.0** installed user-local (no sudo):
  - Binary wrapper: `~/.local/bin/mpv`
  - Tree: `~/.local/opt/mpv-0.37/`
  - Extracted from apt `.deb` + deps (system install blocked by missing sudo password)
- PATH must include `~/.local/bin` for app/`which` discovery
- App also probes `~/.local/bin/mpv` via `dirs::home_dir()` fallback
- `DISPLAY=:0` available for GUI / force-window mpv

## Results

| Check | Result |
|-------|--------|
| `mpv --version` | PASS |
| mpv lavfi testsrc (`vo=null`) | PASS |
| mpv remote mp4 (w3schools) | PASS |
| mpv **visible** window (`--force-window=yes` + lavfi) | PASS (exit 0) |
| `live_recommend_smoke` | PASS |
| `live_categories_smoke` | PASS |
| `live_play_url_mpv_smoke` (recommend → live room → play URL → mpv 6s) | PASS — e.g. room `5050` / earlier `7734200` |
| `player_manager_open_stop_smoke` | PASS |
| Vite shell `http://localhost:1420` (Playwright) | PASS — title `rLive`, sidebar routes present |
| Native `target/debug/rlive` process | PASS — stayed up 4+ min with MESA/EGL warnings only |
| Full in-webview click → Tauri `invoke` → mpv | Not automated (needs WebDriver/xdotool; same Rust path covered by lib smokes) |

### Notes

- Live FLV may log FFmpeg TLS/NAL warnings; process still exited 0 after demux.
- Playwright against Vite alone uses JS fallback `site_list` (all WIP) — **not** the Tauri backend. Real site data only inside the native shell.
- Permanent system install: `sudo apt install mpv` when password available.

### Re-run

```bash
export PATH="$HOME/.local/bin:$PATH"
export DISPLAY=:0

# library e2e (B站 + mpv)
cd src-tauri
cargo test --lib -- --ignored --nocapture

# UI shell + native app
cd ..
bun run dev &          # frontend :1420
bunx tauri dev --config '{"build":{"beforeDevCommand":""}}'
```
