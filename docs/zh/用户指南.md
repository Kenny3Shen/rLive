# rLive 用户指南

## 1. 简介

rLive 是桌面端直播客户端，可在同一应用内浏览、搜索、观看多平台直播，并接收弹幕。

当前站点状态：

| 站点 | 列表 / 搜索 | 播放 | 弹幕 |
|------|-------------|------|------|
| 哔哩哔哩 | ✓ | ✓ | ✓（单条发送需启用本机统一发送开关及有效 Cookie） |
| 虎牙 | ✓ | ✓ | ✓（单条发送需启用本机统一发送开关及有效 Cookie） |
| 斗鱼 | ✓ | ✓ | ✓（个人直播间已成功发送；单条发送仍需本机统一发送开关、完整 Cookie，并按 `chatres` 与真实回显处理） |
| 抖音 | 匿名首屏推荐 / 分类；搜索需登录 Cookie | ✓ | ✓（本地签名直连） |
| Twitch | 首屏直播列表 / 分类 / 搜索 | HLS | ✓（匿名 IRC） |

抖音公开网页的服务端渲染列表当前仅稳定提供首屏。应用不会用 SSR 偏移参数或保存的 Cookie 伪造“加载更多”，以避免重复房间和绕过网页访问验证。保存完整登录 Cookie 后可使用抖音直播搜索，但二维码登录、搜索等网页接口仍可能要求浏览器访问验证。

抖音实时弹幕使用公开 WSS 帧协议，连接地址由短时 MSSDK 签名保护。rLive 在本机计算签名并直连官方 WSS，无需配置外部签名服务。匿名 `ttwid` 会话或完整登录 Cookie 可提高连接可用性。接入边界详见[抖音平台 API 文档](抖音平台API文档.md)。

Twitch 支持直播列表、分类、搜索、房间、HLS 播放与匿名 IRC 弹幕。其公开浏览接口仅可靠提供首屏结果，因此应用不提供翻页，以避免重复结果或绕过接口完整性校验。

## 2. 安装与启动

### Windows（推荐）

1. 使用构建产物：`D:\dev\rLive\src-tauri\target\release\rlive.exe`  
2. 或从源码在 Windows 上执行 `scripts\build-windows.ps1`。  
3. 双击运行即可；**不需要**单独安装 mpv。

### 从源码开发

见仓库根目录 `README.md`：`bun install` → `bun run tauri dev`。

## 3. 界面结构

| 区域 | 说明 |
|------|------|
| 左侧栏 | 首页、关注、分类、历史、IPTV、设置；「设置」上方的单一太阳 / 月亮按钮可在亮色与暗色模式间轮换 |
| 顶栏 | 首页显示站点切换与搜索（主播 / 房间号 / 标题）；其他页面不显示搜索按钮。切换平台时内容区会用短暂渐入过渡，关注页的站点筛选同样居中显示，并额外提供「全部平台」 |
| 房间页 | 左侧纯图标返回、直播间标题、播放器、右侧主播信息，以及「弹幕 / 关注 / 设置」标签 |

## 4. 观看直播

1. 首次启动默认打开哔哩哔哩；之后可在顶栏选择站点。
2. 从「分类」点击分区会打开独立直播列表页；搜索页可切换「全部 / 主播 / 房间号 / 标题」。
3. 房间顶栏左侧提供纯图标返回，内部打开的房间会回到来源页，直接打开房间链接则回到首页；右侧栏顶部显示主播头像、用户名、平台图标、房间号和当前热度。
4. 播放器控制条透明叠放在视频底部。播放期间闲置一段时间会自动隐藏；移动、点击或键盘操作即可再次显示。展开音量、清晰度或线路选项时，控制条会保持显示；焦点位于画面时，可用 `Space` / `K` 播放或暂停、`M` 静音、`F` 全屏。
5. 刷新位于暂停左侧，会重新获取流元数据并重建播放会话；控制栏只保留一个音量入口，点击后展开竖向滑杆，静音按钮位于滑杆底部。「仅播声音」位于同一组控制中，开启后保持音频播放，隐藏视频、飘屏弹幕、SC 和字幕画面，并释放屏幕常亮；再次点击会恢复画面。音视频复用的直播地址仍需传输原始媒体流，因此该模式不保证减少网络流量。
6. 清晰度与线路是控制条右侧的独立选项框（有多路时）。默认开启的「智能线路选择」会通过当前应用代理并发测速，按可用性、首字节耗时和平台顺序选择初始线路；线路菜单会显示测速中、未测速、耗时或不可用状态。当前线路开始正常播放后，不会只因另一线路测速更快而打断播放。
7. 右侧栏、飘屏与全屏均使用图标开关；收起右栏后再次打开，聊天列表仍会保留。点击一条普通弹幕可选择带图标的「复制」或「+1」操作：复制会写入剪贴板；在发送条件已就绪的 B 站、斗鱼或虎牙房间，「+1」会提交完全相同的一条文本，不会附加“+1”。本机提交不等于送达，列表和飘屏只以平台真实回显为准。
8. 默认清晰度、智能线路选择和默认关闭的「软切换实验」可在「设置 → 播放」中调整。软切换仅在同协议换源时尝试保留当前播放器，失败或跨协议时自动完整重建。
9. 抖音可匿名查看推荐/分类首屏并进入房间播放；搜索需要先在「设置 → 账号」通过扫码登录或手动输入保存完整网页 Cookie。推荐和分类不会显示“加载更多”：应用不伪造 SSR 翻页，也不绕过抖音网页访问验证。房间会优先由 SSR 取得内部房间号，再通过公开 reflow 数据解析播放信息。实时弹幕会在本机签名后直连，并解析普通聊天、表情聊天、礼物、点赞、进场与常见社交提示。Twitch 支持直播列表、分类、搜索、房间、HLS 播放与匿名 IRC 弹幕；受公开接口限制，浏览仅可靠支持首屏，不提供翻页。
10. 侧栏「IPTV」先打开频道发现首页，不会在进入时默认打开或播放第一个流。可加载 IPTV-org 官方日更的中文、中国大陆、东亚或综合公开频道分表；如需自定义源，请在「设置 → 网络」保存你有权使用的 HTTP(S) M3U 地址，随后首页会显示「自定义源」。手机与桌面使用相同的来源、更新、搜索、分类和可用状态流程；手机使用紧凑来源菜单与双列频道，桌面使用分段来源切换与多列频道。「更新源」重新下载当前 M3U；「检测可用性」按 URL 去重检测当前搜索/分类结果的前 120 个频道，并逐批显示进度、响应耗时和失败状态。两项网络操作均遵循应用代理。检测结果仅属于当前页面会话，更新或切换来源后会清除；绿色“可用”只说明本次请求取得了媒体数据（HLS 同时通过清单头校验），不保证后续分片、解码、地区授权或持续稳定。点击频道后才进入独立的沉浸式 `/iptv/play` 播放页，并使用 HLS、MPEG-TS 或 FLV 路径；返回会回到保留筛选条件的频道列表。自定义地址只保存在当前设备，不写入路由、历史或配置导入导出。

播放技术说明：应用通过本机 HTTP 代理转发直播流，前端由 `xgplayer`、官方 `xgplayer-flv` / `xgplayer-hls`、Twitch 使用的 `xgplayer-hls.js` / `hls.js` 及 MPEG-TS 兼容插件播放，避免跨域与部分 CDN 限制。播放遥测只在内存中保留有界的首帧、卡顿、缓冲、丢帧和代理聚合指标，不上传、不落盘，也不记录播放地址、Cookie 或 Referer。实现与隐私边界见[播放器技术文档](播放器技术文档.md)。

## 5. 弹幕

### 5.1 连接

进入哔哩哔哩、虎牙、斗鱼、抖音或 Twitch 直播间后，应用会自动连接该站点的弹幕服务。右侧列表显示连接状态与消息；画面上方可显示飘屏弹幕。

- **哔哩哔哩**：在「设置 → 账号 → 哔哩哔哩」选择「扫码登录」或「手动输入」保存 Cookie，否则可能无法拿到弹幕 token。扫码使用手机端确认后只会将 Cookie 保存到本机。连接意外中断后会自动轮换服务器、刷新 token 并重连；系统消息会显示重连进度。
- **虎牙 / 斗鱼**：接收弹幕一般无需 Cookie。单条发送还需在「设置 → 账号」最上方启用「启用发送功能」并保存对应本机 Cookie。斗鱼与虎牙均支持扫码或手动输入；斗鱼当前网页对齐的发送实现已在个人直播间成功验证，每次运行仍以 `chatres(res=0)` 判断平台接收，并以正常收弹幕连接的真实回显决定展示，详见[斗鱼平台 API 文档](斗鱼平台API文档.md)。斗鱼弹幕使用系统 TLS（Windows 为 SChannel），以兼容其 RSA 加密套件。
- **抖音**：在「设置 → 账号 → 抖音」可扫码登录或手动输入保存 Cookie；完整 Cookie 可用于登录态搜索，并改善弹幕会话可用性，但不绕过网页访问验证或开启列表翻页。若二维码返回验证页，请检查应用代理后重试，或在官方网页完成验证后复制完整 Cookie。实时弹幕在本机完成 MSSDK 签名后直连官方 WSS，断线重进会重新签名。
- **Twitch**：弹幕通过匿名 IRC WebSocket 连接。

### 5.2 发送 B站、斗鱼与虎牙弹幕

这是默认关闭的本机授权，以避免误发。在「设置 → 账号」最上方的「弹幕发送」中启用「启用发送功能」后，才会同时允许 B 站、斗鱼和虎牙的手动单条发送与房间右侧自动发送控制；授权开关只保存在当前设备，不会随配置导出或导入，但自动发送的开关、文本与发送间隔仅属于当前房间会话且绝不保存。它不是登录状态：每个平台仍须保存自己的有效 Cookie，并分别通过房间、文本、冷却和平台服务端校验；条件不完整时，该平台房间的发送框与自动发送控制均保持禁用。斗鱼旧协议已替换为当前网页认证与结果确认状态机，并已在个人直播间成功验证；每一条消息仍只在平台确认和真实回显后显示。

播放器功能栏中间的输入框仍是每次只提交一条普通文字弹幕的手动操作。点击发送、按 Enter 或对一条弹幕使用「+1」都会直接提交同一条文本；不提供批量发送、自动回复、礼物/支付或对不确定写入的自动重试。本机提交或平台 `chatres` 都不替代房间真实回显。

在房间右侧「设置」的「自动发送弹幕」仅适用于 B 站、斗鱼和虎牙。共用本机授权、Cookie/可发送状态和文本校验均就绪后，开启时会立即发送首段。文本会将换行和连续空白压缩为一个空格，再按 grapheme 拆分：每段最多 20 个用户可见字符，且不超过当前平台的 UTF-16 上限；片段按顺序发送，末段后从首段继续。发送请求不会重叠，后续发送起始至少相隔发送间隔；该会话参数默认 20 秒，可在 10–3600 秒间调整，修改会重排下一次发送。单个 grapheme 无法容纳在平台上限内时会显示校验错误。编辑文本、切换房间（同时清空该会话文本）、离开页面、关闭应用或任意发送失败都会立即停用本会话自动发送并显示原因；失败片段不会自动重试。

- **哔哩哔哩**：需要包含 `SESSDATA` 与 `bili_jct` 的有效 Cookie。只支持普通滚动、白色文本；后端会校验 Cookie、房间号、控制字符、当前网页端默认的 20 个 UTF-16 码元上限，并按房间施加 3 秒冷却。官方网页会按账号/服务端下发该上限；rLive 暂以调研到的默认值执行。发送请求不会携带 Cookie 跟随重定向。
- **斗鱼**：在「设置 → 账号 → 斗鱼」中可扫码登录或手动保存完整 Cookie。发送会校验数字房间号、非空单行文本与保守的按房间 3 秒冷却；当前实现使用当前网页的登录后加密协商、稳定设备身份、统一代理和 `chatres` / `error` 解析，且已在个人直播间成功验证。完整 Cookie 还需包含账号、设备和弹幕会话字段；缺失时会明确拒绝，而不会伪造设备 ID 或借用普通 JWT。仅在收到 `chatres(res=0)` 并看到真实回显后视为成功，详见[斗鱼平台 API 文档](斗鱼平台API文档.md)。
- **虎牙**：在「设置 → 账号 → 虎牙」中可扫码登录，或手动保存包含数字账号 ID（`yyuid` 或 `udb_uid`）及登录凭据（`udb_n` / `udb_cred` / 扫码产生的 `udb_biztoken`）的 Cookie。发送前会校验解析出的房间、非空、单行且不超过 30 个 UTF-16 单元的文本，并施加短暂的本机冷却。

这三项发送能力只使用当前设备保存的 Cookie。Cookie 不会被记录到日志、导出或上传给其他服务。列表与飘屏仅在收到真实回显后显示；斗鱼还将 `chatres` 作为平台接收信号，与本机提交和房间展示分开报告。

### 5.3 平台真实回显

发送 command 在本机成功返回不会生成本地列表项或飘屏。右侧弹幕栏与飘屏只显示通过正常房间连接收到的平台真实回显，并保留平台原有样式；超时或发送失败仍应以平台直播间的实际状态为准。斗鱼将本地提交、`chatres(res=0)` 和真实回显区分为不同阶段，未知状态不会自动重试。

### 5.4 弹幕设置

路径：进入任意直播间后，使用右侧「弹幕 / 关注 / **设置**」中的 **设置** 标签。显示设置保存在本机，对后续房间同样有效；自动发送是例外，只属于当前房间会话。

| 项 | 说明 |
|----|------|
| 显示区域 | 飘屏可使用的视频高度（10%–100%） |
| 显示行数 | 自动安排轨道，或固定为 1–20 行 |
| 不透明度 | 飘屏文字透明度，拖动实时预览，松手保存 |
| 字号 / 字重 | 影响飘屏与右侧列表基础字号；字重提高亮画面可读性 |
| 速度 | 飘屏滚动速度（逻辑 1–10） |
| 相同内容合并 | 仅飘屏：在合并窗口内合并所有用户发送的相同普通聊天，并显示次数，例如「加油 ×100」；窗口默认 10 秒，可调为 5–30 秒；右侧列表逐条显示 |
| 礼物信息过滤 | 默认开启；隐藏各平台礼物通知及抖音「点赞 × 数量」等互动通知，不影响 SC |
| 屏蔽词 | 每行一词，输入即过滤；聊天、SC 与飘屏共用，并会自动保存 |
| 显示 SC 卡片（B 站） | 默认开启；在播放器左下角显示醒目留言卡片，关闭后立即停止显示 |
| 自动发送弹幕（B 站 / 斗鱼 / 虎牙） | 默认关闭的当前会话开关、文本与发送间隔；间隔默认 20 秒，可设为 10–3600 秒；满足发送条件后立即发送首个经过校验的片段，修改间隔会重排下一次发送 |

所有显示参数在房间内即时生效：滑块拖动时立即预览，松开后保存；开关与字重即时保存。

高弹幕量房间会按动画帧合并列表与 SC 更新，并限制等待队列大小；聊天页签保留有界队列，SC 叠加层最多同时显示 3 张卡片。Canvas 在没有活跃飘屏时停止请求动画帧，收到新消息、调整设置或改变尺寸时自动恢复，以降低 CPU 占用；轨道从顶部依次向下分配。斗鱼的高频进场包与伪装成聊天的“xxx 进入直播间”文本都会在 Rust 层丢弃；所有站点的进场事件也会在前端统一隐藏。

### 5.5 消息类型

| 类型 | 表现 |
|------|------|
| 普通聊天 | 列表 + 飘屏 |
| B 站图片表情 | 与普通聊天一样显示；平台随消息下发图片 URL，应用只加载校验过的 B 站 CDN 资源，并保留文字与表情顺序；飘屏图片加载中或不可用时回退显示原始文字 |
| 进场 | 默认隐藏，避免高频“xxx 进入直播间”干扰 |
| 礼物 | 列表角标「礼物」 |
| 醒目留言 SC | 列表角标「SC」+ 飘屏置顶强调 + 播放器左下卡片 |
| 系统 | 连接状态等，仅列表 |

### 5.6 醒目留言（SC）

哔哩哔哩的 `super_chat` 消息会以紧凑卡片叠放在播放器左下角，最多同时显示 3 张，并按平台给出的置顶时长倒计时退出。发送者头像与名称位于身份区，金额保持醒目；经安全校验的 B 站金额档位主色与底色构成正文带，正文最多显示 3 行。头像缺失、被拦截或加载失败时显示名称首字母；应用不请求用户资料接口。屏蔽词同样作用于 SC。在房间右侧「设置 → 醒目留言」可关闭或重新开启卡片，默认开启；未接入 SC 的平台不显示该设置。

## 6. 本地语音字幕

桌面和 Android 客户端使用 sherpa-onnx streaming Zipformer 在本机生成中英双语直播字幕。进入「设置 → 播放 → 语音字幕」，开启流式字幕；VAD（静音端点检测）和自动标点默认开启，可独立关闭；关闭自动标点时首次启用约下载 `488 MiB`，默认约 `550 MiB`。可选「本地热词」用于主播名、游戏名等专有名词，配置后会使用本地 hotwords 文件增强识别。可选「说话人区分」默认关闭，首次开启会再下载约 `27 MiB` 的 CAMPPlus 声纹模型。字幕更新间隔可在 `0.2–1.0 秒`内按 `0.1 秒`步长调整（默认 `0.2 秒`），字号可在 `12–48 px` 内调整（默认 `20 px`）。等待模型就绪后，进入直播间并点击播放器控制栏的字幕图标即可显示或关闭字幕。

Windows 桌面端可在「推理后端」中选择「自动 / CUDA / CPU」。自动模式优先使用 NVIDIA CUDA，CUDA 运行库或驱动不可用时自动回退 CPU；设置页会显示实际 provider 和回退原因。选择 CUDA 时也会在不可用时回退 CPU。Linux、macOS 和 Android 固定使用 CPU。CUDA provider 会传递给 Zipformer、标点和声纹模型；当前 VAD 是 CPU endpoint 规则，不是独立模型。CPU provider 下 Zipformer 最多 8 个线程，标点与声纹模型最多 2 个线程。配置热词时会切换 modified beam search，未配置时使用 greedy search。关闭总开关会停止采集并释放常驻识别、标点和声纹会话，但不会删除模型文件。

直播音频只在当前设备的 WebView 与 Rust 进程之间传递，不上传到 rLive 服务，也不会保存音频或字幕历史。同一个长生命周期 Zipformer stream 持续接收无重叠 PCM 分块并返回当前 partial；VAD 开启时按静音端点结束语句，关闭时仍按最长 20 秒切分；自动标点开启时 final 和 partial 会恢复中英文标点，关闭时保留原始文本。final 与 partial 自动分行，长文本按播放器宽度自然换行。启用说话人区分后，只有 final 会添加“说话人 N”匿名编号，短句和临界相似度会沿用最近稳定标签，partial 不标注；编号在同一播放会话内保持，切房或切换媒体源时重置。短句、背景音乐、重叠讲话或强噪声可能无标签或不准确，编号不是姓名识别。设备速度低于实时速度时只保留最新待处理分块，以优先保证直播播放和界面响应。构建方式、输入格式、Android 打包、性能参数和已知边界见[本地语音字幕](本地语音字幕.md)。

## 7. IPTV

从左侧栏进入「IPTV」后会到达 `/iptv` 频道发现首页：默认提供 IPTV-org 官方日更的中文语言、中国大陆、东亚和综合公开频道分表。自定义 HTTP(S) M3U 地址只能在「设置 → 网络」保存；配置完成后首页会将其显示为「自定义源」，不再提供自由填写地址的入口。该地址只保存在当前设备，不写入路由、历史或配置导入导出。首页只展示来源、源更新时间、分类、搜索、可用状态与频道列表，进入时不会挂载播放器或默认播放第一个流。内置源不再使用超过应用安全上限的全球总表；支持多关键词按频道名/分组筛选，精确与前缀匹配优先展示，常用分组可一键选择，长列表按需展开。手机端使用紧凑来源菜单和双列频道，桌面端使用分段来源切换和多列频道，但操作名称与状态反馈保持一致。

「更新源」会按照当前应用代理重新下载 M3U，并在成功后更新频道数与时间；触屏设备也可下拉更新。「检测可用性」对当前搜索和分类结果按频道 URL 去重，最多检测前 120 个；前端以每批 24 个提交，原生端进一步限制并发与单项超时，等待媒体首字节并拒绝 HTTP 错误、空响应和伪装成频道的 HTML 页面，HLS 还必须包含 `#EXTM3U` 清单头。完成后可筛选「可用 / 不可用 / 未检测」，失败频道保留原因提示。检测不会启动播放器或占用播放代理，结果不持久化，更新或切换来源后自动清除。“可用”不等于完整播放保证：后续分片、编解码器、地区限制和上游持续稳定仍可能使播放失败。

点击频道后才会进入独立、沉浸式的 `/iptv/play` 播放页，通过本机代理加载 HLS、MPEG-TS 或 FLV 流，以处理嵌套 HLS 清单、分片和常见跨域限制。播放页使用与直播房间相同的透明 React 控制栏，提供重连、播放/暂停、静音/音量、仅播声音、画中画和全屏；播放时控制栏会自动隐藏，移动指针、触摸画面或按键后重新显示。「仅播声音」保持当前频道音频，隐藏视频并释放屏幕常亮，再次点击恢复画面。焦点位于画面时可使用 `Space` / `K` 播放或暂停、`M` 静音、`F` 全屏。使用返回会回到保留来源、分类与搜索条件的频道列表。

节目内容与可用性由上游 M3U 来源决定；应用仅解析并播放配置的流地址。

## 8. 关注与历史

- **关注**：在房间内关注主播，可打标签；右侧「关注」会把直播中的主播排在前面，并在平台名称后通过时钟图标显示已开播时长，不再占用该位置显示房间号。点击卡片可直接切换房间，保留当前标签，无需先退出。通过这里切换后，房间左上角返回会直接回首页，而不会回到上一间直播间。关注页顶栏可筛选「全部平台」或任一平台；列表上方的状态筛选仅有「全部 / 直播中 / 未开播」。页面与房间内列表右下角的浮动按钮可刷新开播状态。
- **历史**：进入过的房间会记入历史，可从历史快速再进。

## 9. 设置摘要

| 分区 | 内容 |
|------|------|
| 外观 | 侧栏「设置」上方只有一个太阳 / 月亮按钮；每次点击在亮色与暗色模式间轮换 |
| 默认清晰度 | 进房优先档位 |
| 智能线路选择 | 默认开启；通过当前应用代理探测多线路，优先可用且首字节更快的来源，并用于故障转移 |
| 软切换实验 | 默认关闭；同协议换源尝试保留播放器，任何失败自动回退完整重建 |
| 本地语音字幕 | 桌面端 / Android sherpa-onnx Zipformer；VAD/自动标点独立开关、本地热词、按需下载约 `488–550 MiB` 识别与标点模型，可选约 `27 MiB` CAMPPlus 匿名说话人区分，`0.2–1.0 秒`更新间隔、自动两行字幕和 `12–48 px` 字号 |
| 网络代理 | 可选 HTTP(S) 代理，如 `http://127.0.0.1:7890` |
| 本地诊断日志 | Windows 发布版写入 `%APPDATA%\rlive\logs\rlive.log`，单文件超过 2 MiB 会保留上一份；日志只记录脱敏失败信息，不写入成功过程、Cookie、令牌或弹幕正文 |
| IPTV 自定义 M3U | 仅在「设置 → 网络」保存有权使用的 HTTP(S) 地址；IPTV 首页仅显示已配置的「自定义源」，地址不写入路由、历史或配置包 |
| 哔哩哔哩 Cookie | 可扫码登录或手动输入；用于只读 API / 接收弹幕，发送功能另需 `SESSDATA` 与 `bili_jct` |
| 弹幕发送 | 位于「账号」页最上方；默认关闭的本机统一开关，同时控制 B 站、斗鱼和虎牙的单条普通文本发送；各平台仍须各自满足 Cookie、房间、文本和冷却校验 |
| 斗鱼 Cookie | 可扫码登录或手动输入；发送需完整的账号、设备和弹幕会话字段。当前网页对齐的发送状态机已在个人直播间验证；运行时仍以 `chatres` 和真实回显判定结果，不能仅凭输入框可用确认送达 |
| 虎牙 Cookie | 支持扫码或手动输入；发送普通弹幕需要 `yyuid` 或 `udb_uid`，以及 `udb_n` / `udb_cred` / `udb_biztoken` |
| 抖音 Cookie | 可扫码登录或手动输入；匿名浏览会自动建立临时 `ttwid` 会话；完整登录 Cookie 可用于直播搜索和弹幕连接，但不绕过网页访问验证或提供列表翻页 |
| 关于 | 提供项目 GitHub 主页跳转 |
| 弹幕设置（房间内） | 见上文；不再放在全局设置页 |
| 配置导入导出 | 关注、标签、历史和可移植设置；**不含 Cookie、自定义 M3U 地址、本机统一弹幕发送授权、ASR 启用状态、VAD/标点/说话人开关、热词或更新间隔** |

Cookie、自定义 M3U 地址、本机统一弹幕发送授权、ASR 启用状态、VAD/标点/说话人开关、热词和更新间隔都只保存在本机 SQLite。导入配置不会覆盖这些设备私有项。请勿提交 Cookie、私有节目源地址或热词配置到 Git 或分享。

## 10. 常见问题

**Q: 语音字幕支持 GPU 吗？**

A: Windows 桌面端支持 NVIDIA CUDA。请在设置中选择「自动」或「CUDA」；自动模式检测不到兼容 GPU、NVIDIA 驱动、CUDA provider 或其依赖时会回退 CPU，并显示实际 provider。Windows 构建默认包含 `sherpa-onnx-v1.13.4-win-x64-cuda.tar.bz2`，需要 Compute Capability 6.1 及以上的 NVIDIA GPU、兼容驱动、CUDA 11.x 和 x86-64 cuDNN 8.x；GTX 1070 Ti 可使用该包，但不能混用 CUDA 12/cuDNN 9。Linux、macOS、Android 仍使用 CPU。详见[本地语音字幕](本地语音字幕.md)。

**Q: 语音字幕延迟较高或偶尔跳过内容？**

A: 将「字幕更新间隔」调到 `0.2 秒`可获得最快刷新；`0.3–0.5 秒`可减少 IPC 调用。Zipformer 的识别上下文保存在同一个 stream 中，不会因分块变短而重置。慢设备只保留最新待处理分块，不会无限排队。启用自动标点后，endpoint final 会恢复标点并与当前 partial 分行；配置热词会增加 modified beam search 开销，性能不足时可减少热词数量。编译期默认参数见[本地语音字幕](本地语音字幕.md)。

**Q: 说话人编号为什么变化或没有显示？**

A: 这是每个播放会话内的匿名声纹聚类，不是姓名识别；切房、换媒体源或关闭字幕会从“说话人 1”重新编号。过短语句可能无法提取声纹，音乐、噪声和多人同时说话也会降低准确率，此时字幕可能不显示编号。Android 开启后会增加 CPU、耗电和发热。

**Q: 字幕字号会影响 ASR 推理性能吗？**

A: 不会。字号只在播放器叠加层渲染时生效，范围为 `12–48 px`，不会改变音频采集、模型或解码参数。

**Q: 斗鱼弹幕提示 HandshakeFailure？**  
A: 请使用包含 `native-tls` 修复后的版本；旧版 rustls 无法与斗鱼弹幕服握手。

**Q: 哔哩哔哩无弹幕？**  
A: 检查 Cookie 是否有效、是否含 `SESSDATA` 等；匿名时部分房间仍可能受限。

**Q: B 站二维码过期或扫后未登录？**
A: 点击「刷新二维码」后重新扫描，并在手机端确认登录；网络异常时可切换到「手动输入」粘贴浏览器 Cookie。

**Q: 虎牙进房闪退？**  
A: 请使用已修复 UTF-8 解析的版本；旧版在中文房间页可能 panic。

**Q: 抖音搜索提示需要登录？**  
A: 在「设置 → 账号 → 抖音」扫码登录，或粘贴完整网页端登录 Cookie 后重试。分类、推荐首屏和房间播放不要求手动登录；推荐和分类当前不提供加载更多。若扫码返回浏览器访问验证页，请检查应用代理后重试，或在已完成验证的网页端复制完整 Cookie。

**Q: 抖音弹幕无法连接？**
A: 先确认房间仍在直播，并尝试重新进入房间以刷新签名与网页会话。若已登录，可在「设置 → 账号 → 抖音」检查 Cookie 是否有效。网络代理异常、地区限制或平台风控仍可能导致连接失败。

**Q: B站发送框为什么不可用？**
A: 先在「设置 → 账号」最上方启用「启用发送功能」，再保存包含 `SESSDATA` 和 `bili_jct` 的 Cookie；发送框只会在 B 站房间显示。

**Q: 斗鱼发送框为什么不可用，或点击后没有弹幕？**
A: 先检查「启用发送功能」和完整本机登录态；斗鱼发送还需要账号、稳定设备和弹幕会话字段。当前实现会分别报告本地提交、平台拒绝和未确认；只有 `chatres(res=0)` 加正常收弹幕连接的真实回显才算成功。请勿对未知状态重复发送，详见[斗鱼平台 API 文档](斗鱼平台API文档.md)。

**Q: 斗鱼发送失败后如何收集诊断信息？**
A: 查看 `%APPDATA%\rlive\logs\rlive.log` 中脱敏的失败阶段与错误码，并以平台真实回显为准；不要分享 Cookie、令牌、签名或弹幕正文。协议、限制与验证记录见[斗鱼平台 API 文档](斗鱼平台API文档.md)。

**Q: 虎牙发送框为什么不可用？**
A: 先在「设置 → 账号」最上方启用「启用发送功能」，再在「虎牙」中扫码登录或手动保存同时包含 `yyuid` 或 `udb_uid`，以及 `udb_n` / `udb_cred` / `udb_biztoken` 的有效 Cookie 后重新进入房间。

**Q: 黑屏无声？**  
A: 尝试换线路/清晰度；确认代理设置未阻断 CDN；退出再进同一房间时应用会刷新播放地址。
