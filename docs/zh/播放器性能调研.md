# 播放器性能调研

更新时间：2026-08-03。本文记录 rLive 当前 xgplayer Web MSE 播放链路的性能判断与后续验证路线。

## 结论

播放器已经迁移到 `xgplayer`：核心统一管理媒体元素和生命周期，FLV / HLS 使用官方协议插件，IPTV 裸 MPEG-TS 使用 xgplayer 兼容插件。Rust 仍只负责本机代理和请求头注入，最终视频解码仍由 WebView2 / Chromium 的 `<video>` 硬件路径承担。

换成 Rust 或 FFmpeg 不会天然提升解码性能，反而会增加 Windows 原生窗口嵌入、D3D 合成、透明控制条/Canvas 弹幕叠层、DLL 分发与编解码器许可等成本。优先顺序应是：**测量 → 调整 xgplayer 协议插件参数 → 按协议补强代理 → 只在 CPU profile 证明必要时研究 Rust transmux PoC**。

## 当前实现与边界

```text
平台播放 URL + Referer / Cookie
  → Rust stream_proxy（localhost、持续字节流转发）
  → xgplayer 3（统一媒体生命周期）
  → xgplayer-flv / xgplayer-hls.js / MPEG-TS 兼容插件
  → WebView2 MediaSource / <video>（浏览器硬件解码）
  → React 控制层 + Canvas 弹幕
```

当前播放器已启用：

- `xgplayer-flv` 的低延迟 MSE、缓冲回收、起始空洞跳转和有界重试；
- `xgplayer-hls.js` 的 Worker、低延迟模式和 30 秒回退缓冲；
- IPTV 裸 MPEG-TS 兼容路径继续启用延迟追赶与 SourceBuffer 回收；
- 进房时重建 `<video>` 和 MediaSource，避免旧流的黑屏状态；
- Rust 代理复用一个 reqwest 客户端与连接池，连续 HTTP-FLV 不会为每个 chunk 重建 TLS。

目前还没有 `getVideoPlaybackQuality()`、统一下载统计、首帧时间或卡顿计数的运行时遥测。因此现在不能严谨断言卡顿来自 JS transmux、视频解码、网络、MSE 或 Canvas。

## 推荐路线

### 1. 先补可观测性，再调参数

每个播放会话至少采样下列指标，并按平台、清晰度、FLV/TS、WebView2 版本聚合：

| 指标 | 用途 |
|------|------|
| 进房到 `playing` 的时间 | 首帧 / 启动体验 |
| `video.getVideoPlaybackQuality()` 的总帧与丢帧 | 判断解码 / 渲染压力 |
| `buffered.end - currentTime` 与 live edge | 缓冲量和实际延迟 |
| `waiting` / `stalled` 次数与时长 | 重缓冲 |
| xgplayer `statistics_info` / 协议插件统计 | 网络吞吐、解复用侧压力 |
| JS long task、Canvas 帧预算 | 主线程竞争 |

验收时不要只看“能播放”。建议至少比较首帧 P50/P95、每分钟卡顿、丢帧比例、端到端延迟和 CPU 占用；若指标没有改善，不推广实验配置。

### 2. A/B 验证协议插件缓冲与低延迟参数

不同平台、清晰度和网络条件对缓冲大小与延迟追赶的需求不同。应通过 xgplayer 插件配置做小范围 A/B，而不是全局套用一组激进参数。

建议做成默认关闭的实验开关，只在 Windows WebView2 真机测试后扩大：

1. 分别在 1080p / 60fps、普通画质、弹幕高峰和网络抖动下测量；
2. 对网络稳定的 FLV 源逐步降低 `targetLatency`，网络不稳时增加回退缓冲；
3. HLS 分别比较低延迟模式、直播同步位置和回退缓冲长度；
4. 首帧、错误率或卡顿变差时回退当前配置。

不能把最低缓冲设为全局默认：它会降低延迟，但也会在网络抖动时更容易卡顿。

### 3. 持续验证 HLS 代理

当前本机代理已经能够改写 HLS 主/子清单、密钥和分片，`xgplayer-hls.js` 负责播放；Twitch 的短时签名失效会触发播放地址刷新。后续仍需覆盖更多清单写法与上游响应行为。

若产品需要通用 HLS：

- 持续验证 path/query-aware 的清单与分片 URL；
- 转发 Range、状态和必要响应头；
- 覆盖加密密钥、重定向、短时签名和多级清单；
- 保持低延迟 FLV 走独立协议插件，避免错误解析 HLS 文本。

### 4. Rust 只作为有条件的 transmux 实验

可考察的 Rust 起点包括 [`transmux`](https://crates.io/crates/transmux)（MPEG-TS → CMAF/fMP4）和 [`scuffle-flv`](https://crates.io/crates/scuffle-flv)（FLV demux）。但它们不会直接把数据送进 WebView 的 SourceBuffer；完整生产链还需处理 AVC/AAC/HEVC codec config、时间戳、断流恢复、背压和 JS/Worker 到 MSE 的交接。

因此仅在 profile 明确显示 JS transmux 是 CPU 热点且 Worker 不足时，再以独立 TS 小样本进行 PoC。评估条件是实测 CPU 和卡顿改善能覆盖 IPC / localhost copy / 维护成本，而不是“Rust 理论上更快”。

### 5. 不建议近期采用原生播放器或 WebCodecs 重写

| 方案 | 结论 |
|------|------|
| FFmpeg / libmpv / GStreamer / VLC | 不作为近期方向。需要原生 DLL、许可证治理、HWND/D3D 叠层和 Tauri UI 合成，不能自然复用当前弹幕与控制层。|
| `ffmpeg-next` | 它是 FFmpeg C API 封装且项目自称 maintenance-only；不是“纯 Rust 解码器”。|
| WebCodecs | 是低层 codec API，输入前仍需 demux，且需要自建 A/V 同步、渲染和回退，适合作为未来实验而非 mpegts 直接替换。|

## 决策表

| 症状 / 目标 | 首选动作 | 不应先做的事 |
|-------------|----------|----------------|
| 高 CPU、Canvas 与播放同时卡 | 加指标，A/B `enableWorker` | 立刻引入 FFmpeg |
| 网络稳但直播延迟高 | 按平台测试关闭 stash | 全局关闭 stash |
| HLS 播放失败 | 检查代理改写与 xgplayer HLS 事件 | 把 m3u8 当作连续 FLV/TS |
| profile 证明 transmux 占主要 CPU | Rust transmux 小范围 PoC | 全面替换视频解码 |
| 要录制、DRM、特殊 codec | 单独评估 native sidecar | 把它伪装成性能优化 |

## 参考

- [xgplayer 指南](https://h5player.bytedance.com/en/guide/)：核心播放器与协议插件配置。
- [DTV](https://github.com/chen-zeong/dtv)：Tauri / React 中的 xgplayer FLV/HLS 集成参考。
- [mpegts.js README / API](https://github.com/xqq/mpegts.js)：IPTV 裸 MPEG-TS 兼容插件的底层参数。
- [Media Source Extensions](https://w3c.github.io/media-source/)：Dedicated Worker 中的 MediaSource / handle 能力。
- [MDN WebCodecs API](https://developer.mozilla.org/en-US/docs/Web/API/WebCodecs_API)：低层 codec API 仍需要 demux。
- [`transmux`](https://crates.io/crates/transmux)、[`scuffle-flv`](https://crates.io/crates/scuffle-flv)：Rust 容器层实验候选。
- [`ffmpeg-next`](https://github.com/zmwangx/rust-ffmpeg)：FFmpeg FFI 封装的发布与维护边界。
