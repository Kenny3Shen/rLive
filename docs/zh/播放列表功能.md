# 播放列表功能

## 概述

播放列表功能允许用户连续播放多个视频分集，支持正序/倒序播放、自动播放下一集等特性。参考了 [PiliPlus](https://github.com/bggRGjQaUbCoE/PiliPlus) 的实现。

## 功能特性

### 1. 播放列表管理

- **播放全部**：从第一集（或倒序时的最后一集）开始播放整个列表
- **加入列表**：从当前集开始设置播放列表
- **正序/倒序切换**：支持正序和倒序播放模式
- **播放进度显示**：显示当前播放位置（如 "3 / 12"）
- **列表连播**：从搜索结果或 UP 主投稿列表点开任意视频，该列表在点击时刻的快照即成为播放列表（后续加载更多不影响已建立的列表），从被点的视频开始连播
- **合集优先**：稿件属于 UGC 合集（ugc_season）时，合集自动成为播放列表（覆盖搜索/投稿快照），自动连播与「下一个」沿合集顺序走；侧栏默认停在相关视频，「合集」页签在页签条最右；PGC 分集不受影响

### 2. 自动播放下一集

- 视频播放结束后，自动跳转到下一集（可在设置中关闭）
- 延迟 1 秒后开始播放，给用户反应时间

### 3. 跳转入口

- **播放下一个按钮**：控制条播放/暂停旁的「下一个」按钮（列表有后续项时出现）
- **键盘快捷键**：N 或 ] 播放下一集，P 或 [ 播放上一集

### 4. 播放偏好

通过播放器控制栏的设置菜单可以配置（偏好持久化到 localStorage `video-playlist`）：
- 循环播放：开启/关闭。开启后播完当前视频从头重播，**优先于**自动播放下一集；单视频（无列表）同样可用
- 自动播放下一集：开启/关闭（仅列表多于一项时出现）
- 倒序播放：开启/关闭（对合集与列表连播同样生效）

一集播完后的动作由 `videoEndedAction(loopPlayback, autoPlayNext, hasNext)` 决定，优先级为循环 > 连播 > 停住；没有下一集时连播退化成停住。

## 技术实现

### 核心文件

1. **`src/features/video/playlistStore.ts`**
   - 播放列表状态管理（使用 Zustand）
   - 播放列表项数据结构
   - 播放顺序控制
   - 用户偏好持久化（localStorage）

2. **`src/features/video/VideoPlayerPage.tsx`**
   - 自动播放下一集逻辑
   - 键盘快捷键处理
   - 播放列表位置同步
   - 播放列表设置菜单
   - 合集接管：稿件详情带 ugc_season 时用合集替换播放列表

3. **`src-tauri/src/sites/bilibili/video.rs`**
   - `parse_archive` 解析 ugc_season，各分区分集展平为合集列表

4. **`src/features/video/VideoSidebar.tsx`**
   - 分集列表增强
   - 播放控制按钮（播放全部、加入列表、正序/倒序）
   - 播放列表项转换

### 数据模型

```typescript
type PlaylistItem = {
  id: string;              // 唯一标识：`${bvid}_${cid}`
  bvid: string;            // B 站视频 ID
  cid: number;             // 分 P/分集 ID
  epId: string | null;     // PGC 分集 ID（UGC 为 null）
  aid: string;             // 稿件 av 号
  title: string;           // 标题
  index: string;           // 集号或分 P 序号
  duration: number;        // 时长（秒）
  cover?: string;          // 封面 URL
};
```

### 状态管理

播放列表状态通过 `usePlaylistStore` 管理：

- `items`：当前播放列表
- `currentId`：当前播放项 ID
- `reversed`：播放顺序（true = 倒序）
- `autoPlayNext`：是否自动播放下一集
- `loopPlayback`：是否循环播放当前视频（优先于 `autoPlayNext`）

用户偏好（`reversed`、`autoPlayNext`、`loopPlayback`）持久化到 localStorage，播放列表状态在会话期间保持。

### 播放流程

1. 用户点击「播放全部」「加入列表」，或从搜索/投稿列表点开任意视频（快照即列表），或播放合集中的稿件（合集接管列表）
2. 分集/列表条目转换为 `PlaylistItem[]`
3. 设置播放列表到 store
4. 导航到目标视频播放页
5. 播放页同步当前播放项到 store；稿件属于合集且未接管时用合集替换列表（以 bvid 定位当前项，链接可能没带 cid）
6. 视频结束时检查 `autoPlayNext` 和 `nextItem`
7. 自动跳转到下一集（如果有）

## 用户界面

### 合集页签（UGC）

稿件属于 UGC 合集时，右侧栏自动出现并选中「合集」页签：合集标题 + 共 N 个 + 分集列表（序号/标题/时长，当前项高亮），点任意分集直接跳转；打开页签或连播换集时列表自动滚动到当前分集居中位置。

### 分集列表控制栏

在 PGC 分集列表顶部显示：

```
[播放全部] [加入列表]     [倒序/正序图标]
```

### 播放器控制栏

- 中间位置显示播放进度（如 "3 / 12"）
- 设置菜单中新增「播放列表设置」选项

## 限制与约束

1. **仅支持 PGC 分集**：当前实现仅针对番剧/影视的分集列表，UGC 分 P 功能待后续扩展
2. **播放列表不持久化**：播放列表状态仅在会话期间保持，刷新页面后需要重新设置
3. **单一播放列表**：不支持多个播放列表或播放列表队列

## 未来改进

- [ ] 支持 UGC 视频的分 P 播放列表
- [ ] 播放列表持久化（保存到本地数据库）
- [ ] 支持自定义播放列表（跨视频）
- [x] 播放历史记录与断点续播
- [ ] 播放列表面板（独立抽屉/弹窗）
- [ ] 循环播放模式
- [ ] 随机播放模式

## 参考资料

- [PiliPlus - EpisodePanel 实现](https://github.com/bggRGjQaUbCoE/PiliPlus/blob/master/lib/pages/episode_panel/view.dart)
- [PiliPlus - MediaListPanel 实现](https://github.com/bggRGjQaUbCoE/PiliPlus/blob/master/lib/pages/video/medialist/view.dart)
