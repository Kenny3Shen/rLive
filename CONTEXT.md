# rLive 领域上下文

## 播放恢复会话

播放恢复会话是从直播间详情解析当前播放源，并在清晰度切换、线路切换、播放失败、播放地址过期和稳定播放之间维护有界恢复状态的过程。

播放恢复会话拥有播放元数据的获取与缓存同步、用户线路记忆、重试与续签预算、过期异步结果失效，以及当前播放源的选择。Web 播放 transport 不属于播放恢复会话；它负责代理和媒体生命周期，并向播放恢复会话报告播放成功、流结束、解码失败和播放地址失效等事实。

每个播放器实例拥有独立的播放恢复会话。不同实例可以通过 TanStack Query 复用播放元数据缓存，但不共享重试与续签预算、timer、generation、当前清晰度或当前线路；一个实例的播放失败不得消耗或改写另一个实例的恢复状态。

用户主动切换清晰度、切换线路或手动刷新时，该 intent 的优先级高于自动恢复。播放恢复会话必须取消待执行的自动恢复 timer、递增 intent generation，并使旧 generation 的元数据结果和延迟动作失效；自动恢复不得在稍后覆盖用户的明确选择。

Web 播放 transport 只向播放恢复会话报告已经发生的事实，例如开始播放、流结束、解码失败、HTTP 状态和协议错误。重建当前媒体、续签播放地址、降低清晰度、切换线路、延迟重试或终止恢复均由播放恢复会话根据这些事实、站点规则和剩余预算决定；transport 不跨 seam 输出带有恢复决定的字段。

各直播站点的恢复差异属于播放恢复会话的内部规则，不为每个站点建立独立恢复 adapter。只有出现第二种需要独立替换这些规则的真实 implementation 时，才为站点恢复策略建立新的 seam。

播放恢复会话的核心 implementation 不依赖 React 生命周期。React adapter 负责创建和销毁会话、订阅会话 snapshot，并转发用户 intent 与 transport facts；元数据获取和 cache 同步仍由会话拥有，但通过可替换 adapter 执行。

播放恢复会话的首轮架构改造保持现有产品行为，不同时调整各站点重试次数、恢复延迟、线路记忆、Twitch 解码降级、播放地址续签预算或用户提示。测试通过会话 interface 覆盖完整场景；只有 storage、metadata 等 adapter 保留独立契约测试。

## 媒体生命周期会话

媒体生命周期会话是从一个已解析的播放源开始，经由 `stream_proxy`、xgplayer 协议插件和 HTML media element，直到播放器销毁的浏览器端资源生命周期。它拥有 proxy session generation、xgplayer 创建与销毁、协议内恢复、自动播放、媒体状态、音量、全屏和画中画；页面舞台布局、手势仲裁和业务导航不属于媒体生命周期会话。

直播房间与 IPTV 共用同一个媒体生命周期 module。直播 adapter 把媒体事实交给播放恢复会话决定重试、换线、续签或降画质；IPTV adapter 维护频道内的有界自动重连预算。两个 adapter 可以选择不同的缓冲、软切换、临时空源保留和遥测 profile，但不得各自重新实现 proxy 或 xgplayer 生命周期。

播放恢复会话和媒体生命周期会话是相邻但独立的概念：播放恢复会话选择 `PlayUrl` 并拥有业务恢复决策，媒体生命周期会话消费 `PlayUrl` 并只报告已发生的 transport facts。任何一方都不得跨 seam 直接修改另一方的预算或内部 generation。

每个直播或 IPTV 播放器实例拥有独立的媒体生命周期状态和控制状态。底层 `stream_proxy` 操作通过 module 内部共享的串行队列保持有序，但队列不转移实例所有权；旧 generation 的清理只能停止自己拥有的 proxy session。

媒体生命周期合并首轮保持现有直播恢复策略、IPTV 两次自动重连及其延迟、各自的缓冲 profile、音量会话语义、全屏与画中画兼容行为。测试通过共享 module interface 覆盖资源顺序与控制状态，并通过两个 adapter 的场景验证差异。
